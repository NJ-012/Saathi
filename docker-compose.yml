# Optional — only include if it saves setup time for the demo.
# Scaffold placeholder; service definitions pending implementation.
version: "3.9"
services:
  backend:
    build: ./backend
    ports:
      - "4000:4000"
    env_file:
      - .env
  dashboard:
    build: ./dashboard
    ports:
      - "5173:5173"
